<div class="main-container">
    <p>Download Resume</p>
    <div class="content-container">
        <img src="/download-pdf-icon.svg" alt="download icon"/>
        <p><a href="/resume.pdf">Download Color</a></p>
    </div>
    <div class="content-container">
        <img src="/download-pdf-icon.svg" alt="download icon"/>
        <p><a href="/resume-bw.pdf">Download B&W</a></p>
    </div>
</div>

<style>

.main-container {
    visibility: hidden;
    position: fixed;
}
.main-container p {
    
}
.content-container {
    display: flex;
    flex-direction: row;
    align-items: center;
}
.content-container img {
    height: 2rem;
    width: 2rem;
    margin-right: 0.5rem;
}
.content-container p {
    color: #f9d8d2;
    line-height: 0.7rem;
    font-size: clamp(0.2rem, 1.8vh, 1rem);
    font-size: clamp(0.2rem, 1.8svh, 1rem);
    text-wrap: balance;
    text-align: center;
    line-height: 1rem;
    font-weight: normal;
    margin-top: 0;
}
.content-container p a {
    color: #f9d8d2;
    text-decoration: none;
}
.content-container p a:hover {
    text-decoration: underline;
}

@media (min-width: 769px) {
    .main-container {
        visibility: visible;
        position: relative;
        background: #5923c8;
        border-radius: 1rem;
        display: flex;
        flex-direction: column;
        gap: 1rem;

        width: 18vw;
        height: 16vh;
        max-height: 19vh;
        max-height: 19svh;

        padding: 0.5rem;
        padding-top: 0.1rem;
        padding-bottom: 0.1rem;

        margin-left: 1rem;
        margin-right: 1rem;
        margin-bottom: 0;
        margin-top: 3vh;
        margin-top: 3svh;

        font-size: clamp(0.2rem, 1.7vh, 1rem);
        font-size: clamp(0.2rem, 1.7svh, 1rem);
        font-family: DejaVu Sans Mono, monospace, system-ui;
    }
    .main-container p {
        margin: 0;
        margin-top: 1rem;
        text-align: left;
        text-wrap: balance;
        color: #f9d8d2;
        font-weight: bold;
    }
    .content-container p {
        color: #f9d8d2;
        line-height: 0.7rem;
        font-size: clamp(0.2rem, 1.8vh, 1rem);
        font-size: clamp(0.2rem, 1.8svh, 1rem);
        text-wrap: balance;
        text-align: center;
        line-height: 1rem;
        font-weight: normal;
        margin-top: 0;
    }
}

</style>